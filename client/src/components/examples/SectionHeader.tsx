import SectionHeader from '../SectionHeader';

export default function SectionHeaderExample() {
  return (
    <SectionHeader
      title="Featured Projects"
      subtitle="A selection of projects I've worked on, from full-stack applications to machine learning research."
    />
  );
}
